# This file is used to configure npm.
# For example, you can set a default registry:
# registry=https://registry.npmjs.org/

# Or set a proxy:
# proxy=http://proxy.example.com:8080/

# Or disable strict SSL for corporate proxies (not recommended for production):
# strict-ssl=false
